module {
	class IntIterator {
		private var min: Int
		private var max: Int
		function hasNext(): Bool {
			return this.min < this.max
		}

		function next(): Int {
			return this.min++
		}

		new(min: Int, max: Int) {
			this.min = min
			this.max = max
		}
	}
}
