module list {
	private class ListIterator< T > {
		private var head: list.ListNode< T >
		@maybeUsed
		function hasNext(): Bool {
			return this.head != null
		}

		@maybeUsed
		function next(): T {
			var val: T = this.head.item
			this.head = this.head.next
			return val
		}

		new(head: list.ListNode< T >) {
			this.head = head
		}
	}
}
