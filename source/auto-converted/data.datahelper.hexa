module data {
	class DataHelper {
		static function varName(v: data.Node): String {
			if (v.index == 20) {
				let data.Node.TVar(name, _, _, _) = v
				return name
			} else throw new HaxeError('varName got not a TVar, but `' + Std.string(v) + '`')
		}

	}
}
