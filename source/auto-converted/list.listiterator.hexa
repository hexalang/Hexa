module list {
	private class ListIterator< list.T > {
		private var head: list.ListNode< list.T >
		@maybeUsed
		function hasNext(): Bool {
			return this.head != null
		}

		@maybeUsed
		function next(): list.T {
			var val: list.T = this.head.item
			this.head = this.head.next
			return val
		}

		new(head: list.ListNode< list.T >) {
			this.head = head
		}
	}
}
